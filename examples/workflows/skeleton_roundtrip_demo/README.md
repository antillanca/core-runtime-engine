# skeleton_roundtrip_demo

Demonstrates the complete adapter onboarding loop:

create skeleton -> generate fixture -> validate -> certify -> compliance.

## Run

```bash
python examples/workflows/skeleton_roundtrip_demo/run_demo.py
```

The output is deterministic JSON.
